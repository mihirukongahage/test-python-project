"""
Setup script for backward compatibility.
Modern projects should use pyproject.toml, but this file provides
compatibility with older tools.
"""
from setuptools import setup

# All configuration is in pyproject.toml
setup()
